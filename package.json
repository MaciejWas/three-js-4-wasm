{
  "name": "three-js-4-wasm",
  "version": "0.0.1",
  "description": "Thin wrapper aroung THREE.js which can be easily called from WASM",
  "main": "index.ts",
  "scripts": {
    "build": "tsc --outDir dist --target es6 --module commonjs --declaration index.ts"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@types/three": "^0.177.0",
    "three": "^0.177.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
